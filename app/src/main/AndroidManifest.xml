<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.formula1_factory">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ManufactureActivity" />
        <activity android:name=".FactoryOrdersActivity" />
        <activity android:name=".ComponentListActivity" />
        <activity android:name=".RoutesActivity" />
        <activity android:name=".WorkstationListActivity" />
        <activity android:name=".OpenMarketOrderActivity" />
        <activity android:name=".OpenFactoryOrderActivity" />
        <activity android:name=".MarketOrdersActivity" />
        <activity android:name=".OpenComponentActivity" />
        <activity android:name=".OpenRouteActivity" />
        <activity android:name=".OpenWorkstationActivity" />
        <activity android:name=".OpenProductActivity" />
        <activity android:name=".OpenCartActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>